import React from 'react';
import classes from './Chart.module.css';

const Chart = (props) => {
  return (
    <div className={classes.Container}>
      <div className={classes.Chart}>chart content</div>
    </div>
  );
};

export default Chart;
